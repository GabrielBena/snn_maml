[project]

name = "snn_maml"

version = "0.1.0"

requires-python = ">=3.7"

license = {file = "LICENSE"}

keywords = ["snntorch", "eventprop", "pytorch"]

dependencies = [ 
    "numpy",
    "matplotlib",
    "seaborn",
    "wandb", 
    "tqdm",
    "ipykernel",
]

[build-system]
requires = ["setuptools>=43.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["snn_maml"]